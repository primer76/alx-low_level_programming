#include <stdio.h>

/**
 * main - Prints single digit of base 10 starting from 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int l;

	for (l = 48; l < 58; l++)
	{
		putchar(l);
	}
	putchar('\n');
	return (0);
}
