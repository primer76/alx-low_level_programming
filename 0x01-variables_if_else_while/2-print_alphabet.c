#include <stdio.h>

/**
 * main - lists alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char x;

	if (x = 'a'; x <= 'z'; x++)
	{
		putchr(x)
	}
	putchr('\n');
	return (0);
}
