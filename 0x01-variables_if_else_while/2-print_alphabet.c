#include <stdio.h>

/**
 * main - lists alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char = alphabets;
	alphabets = 'a';

	while (alphabets <= 'z')
	{
		putchar("%c ", alphabets);
		alphabets++;
	}
	return (0);
}
