#include "main.h"

/**
 * _abs - computes absolute value of interger
 * @n : code to ne checked
 * Return: abolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return ('n');
	}
	return ('-n');
}
